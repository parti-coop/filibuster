.pull-right
  - if controller_name != 'sessions'
    = link_to new_session_path(resource_name), class: 'btn btn-info' do
      %i.fa.fa-sign-in
      로그인
  - if devise_mapping.registerable? && controller_name != 'registrations'
    = link_to new_registration_path(resource_name), class: 'btn btn-info' do
      %i.fa.fa-user-plus
      가입
